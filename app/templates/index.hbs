<h1>Which Ben?</h1>

<div class='filtered-bens-list'>
  {{#if currentQuestion}}
    {{component currentQuestion setFilter='setFilter'}}
  {{/if}}

  {{#if filteredBens}}
    {{#each ben in filteredBens}}
      {{ben-card ben=ben}}
    {{/each}}
  {{else}}
    {{#each ben in model}}
      {{ben-card ben=ben}}
    {{/each}}
  {{/if}}
</div>

{{#if currentQuestion}}
  {{component currentQuestion setFilter='setFilter'}}
{{/if}}

{{#if filteredBens}}
  {{#each ben in filteredBens}}
    {{ben-card ben=ben}}
  {{/each}}
{{else}}
  {{#each ben in bens}}
    {{ben-card ben=ben}}
  {{/each}}
{{/if}}
